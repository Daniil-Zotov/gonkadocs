name: Sync roadmap.md from gonka repo

on:
  schedule:
    - cron: '40 */1 * * *'   # every hour at :40
  workflow_dispatch:

permissions:
  contents: write

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout my repo
        uses: actions/checkout@v4

      - name: Download roadmap file
        run: |
          mkdir -p docs/community/roadmap
          curl -fSL \
            "https://raw.githubusercontent.com/gonka-ai/gonka/main/proposals/gonka-network-development-roadmap.md" \
            -o docs/community/roadmap/gonka-network-development-roadmap.md

      - name: Regenerate AI-agent files (llms.txt, llms-full.txt)
        run: |
          python3 buildtools/generate-llms.py
          python3 buildtools/generate-llms-full.py

      - name: Commit & push if changed
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/community/roadmap/gonka-network-development-roadmap.md docs/llms.txt docs/llms-full.txt
          if git diff --cached --quiet; then
            echo "No changes — nothing to push"
          else
            git commit -m "chore: sync roadmap + regenerate llms files from gonka-ai/gonka@$(date -u +'%Y-%m-%dT%H:%MZ')"
            git push
          fi
